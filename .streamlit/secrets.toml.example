# 견본 파일입니다. 실제 키를 이 파일에 적지 마세요(이 파일은 저장소에 올라갑니다).
#
# Streamlit Cloud : 앱 설정(Settings) > Secrets 칸에 아래 한 줄만 붙여 넣습니다.
# 내 PC에서 쓰기 : 이 파일을 .streamlit/secrets.toml 로 복사해 채웁니다(secrets.toml은 .gitignore 대상).
#                  또는 기존처럼 프로젝트 폴더의 .env 를 그대로 써도 됩니다.
UPSTAGE_API_KEY = "여기에_키"
